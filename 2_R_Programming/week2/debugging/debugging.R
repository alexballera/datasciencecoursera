# traceback: imprime la función que tiene el error, el error más reciente

mean(x)
traceback()

# debug: abre el navegador y depura la función línea por línea

debug(lm)
lm(x - y)

# recover: nos muestra opciones en las diferentes funciones para depurar

options(error = recover)
read.csv("nohayarchivo")
